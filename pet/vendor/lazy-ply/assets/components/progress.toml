# progress.toml — Linear progress stylesheet (like CSS).
# Unset fields fall back to the M3 theme in progress.rs.

track_height = 4.0     # bar thickness px
radius = 2.0           # track corner radius
track_color = 0xE6E0E9 # unfilled portion
fill_color = 0x6750A4  # filled portion
