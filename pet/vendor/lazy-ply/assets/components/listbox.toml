# listbox.toml — ListBox stylesheet (like CSS).
# Unset fields fall back to the M3 theme in listbox.rs.

item_height = 48.0     # row height px (also drives the visible-area height)
radius = 4.0           # container corner radius
border = 0xCAC4D0      # container outline
font_size = 16         # row label size
