1. 总体架构设计
你是一个资深 Rust GUI 框架架构师。

分析 lazy-ply 项目目标：

目标：
实现类似 Flutter Widget 的组件组合体验，但基于 ply-engine。

期望 API：

侧边栏({
    启动()
    设置()
    关于()
    作者()
})

日志面板()

日志进度条()

要求：
- 组件高度封装
- 用户只组合高级组件
- 组件内部管理自己的子组件
- 组件拥有自己的默认主题、配置、性能优化
- 子组件自动包含
- 支持第三方开发组件

请设计整体架构：
1. Component Trait
2. Widget Tree
3. Composite Component
4. Component Registry
5. Layout System
6. Theme System
7. TOML 配置作用

不要写代码，先输出架构方案。
2. 设计 Rust Component Trait
设计一个 Rust UI Component Trait。

目标：

用户：

Sidebar()

内部：

Sidebar
 ├── StartButton
 ├── SettingButton
 ├── AboutButton


要求：
- 支持复合组件
- 支持生命周期
- 支持事件处理
- 支持状态更新
- 支持渲染
- 支持布局计算
- 基于 ply-engine

参考 Flutter Widget / React Component，
但是必须符合 Rust 所有权模型。

输出：
- Trait 设计
- 生命周期
- 数据流
- 示例代码
3. 设计 UI DSL
设计一个 Rust UI DSL。

目标实现：

ui! {
    Sidebar {
        Start()
        Settings()
        About()
    }

    LogPanel()

    ProgressDialog()
}


要求：
- 使用 Rust macro_rules 或 proc_macro
- 自动生成 Widget Tree
- 支持嵌套组件
- 支持属性：

ProgressDialog(
    theme="nvim"
)

分析：
- macro_rules 是否可行
- proc_macro 是否更适合
- 最佳实现方案

输出设计，不需要完整代码。
4. 组件自动发现系统
设计一个 Rust UI 组件自动注册系统。

目标：

用户只需要：

use lazy_ply_components::*;

Sidebar()


无需手动：

mod sidebar;
mod button;
mod theme;


组件包：

sidebar/
 ├── sidebar.rs
 ├── sidebar.toml
 └── children/


要求：
- Rust 编译期注册
- 支持第三方 crate
- 支持组件元数据
- 类似 Flutter pub.dev / npm component


分析：
inventory/linkme/build.rs/proc_macro 哪个方案最佳。
5. TOML 设计
设计 lazy-ply 的组件 TOML 标准。

注意：

TOML 不是 UI 编写语言。

作用：

组件描述文件。


需要支持：

component.toml

包含：

- 名称
- 版本
- 默认主题
- 默认布局
- 性能参数
- 资源
- 子组件声明


例如：

日志组件：

最大显示10000行
自动虚拟列表
nvim 默认主题


设计一个长期可扩展 schema。
6. MVP 实现路线
你是 Rust 开源项目负责人。

lazy-ply 目标：

创建一个基于 ply-engine 的高级 UI 组件框架。

请制定 MVP 开发路线。

限制：
一个人开发。

优先：
1. 能写组件
2. 能组合组件
3. 能渲染
4. 能扩展

不要提前设计复杂功能。

输出：
- 第一周实现什么
- 第二阶段实现什么
- 哪些功能不要做
7. 给 AI Coding Agent 的版本

如果你用 Claude Code / Codex：

阅读 https://github.com/lilyco-42/lazy-ply

不要重新设计项目。

理解目标：

lazy-ply 希望提供：

Sidebar({
    Start(),
    Settings(),
    About()
})

LogPanel()

ProgressDialog()


这种高级 UI 组件开发体验。


请先分析当前代码：
1. 当前 ply-engine 能力
2. 缺少哪些抽象层
3. 最小修改方案

不要直接写代码。

先输出技术方案和文件结构。
