//! Shared code for the `demo` binaries: the M3 component library, theme and
//! font resolution. Both `demo` (src/main.rs) and `demo_menu` (src/bin/demo_menu.rs)
//! depend on this crate.

pub mod components;
pub mod fonts;
pub mod theme;
